const config = {
  baseStyle: {
    requiredIndicator: {
      color: 'brand.500',
      fontSize: '20px',
      fontWeight: 'bold',
      position: 'absolute',
    },
  },
};

export default config;
